# Jekyll for GitHub Pages (optional: for local preview)
# Ruby 3.x recommended; on Ruby 2.6 use: gem "jekyll" and skip github-pages if deps fail
source "https://rubygems.org"

gem "jekyll"
gem "github-pages", group: :jekyll_plugins
