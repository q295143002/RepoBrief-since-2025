## ADDED Requirements

### Requirement: Excel Export
The system SHALL provide an "Export CSV/Excel" button that downloads the current filtered list of projects.

#### Scenario: Exporting filtered data
- **WHEN** user filters by "AI" and clicks "Export"
- **THEN** system generates and downloads an Excel/CSV file containing only the project data matching the "AI" filter.
