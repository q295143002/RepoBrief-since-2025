## 1. 基础设施与数据接口

- [ ] 1.1 更新 `types/repo.ts`，定义标签计数及分组的数据结构。
- [ ] 1.2 在 API 端或数据加载层实现 `getTagCounts` 函数，统计各标签下的项目数量。
- [ ] 1.3 实现 `groupTagsAlphabetically` 逻辑，将所有标签按首字母进行 A-Z 分组并排序。

## 2. 头部 (Header) 优化

- [ ] 2.1 开发 `GithubNav` 组件，使用 `antd` 的按钮和 `Lucide` 图标，链接指向：`https://github.com/q295143002/RepoBrief-since-2025`。
- [ ] 2.2 实现星数获取逻辑，调用 GitHub API 或从预加载数据中展示星数（如 43.5k）。
- [ ] 2.3 将 `GithubNav` 注入 `Header` 组件，并适配深色/浅色模式样式。

## 3. 项目说明模块 (Hero Section)

- [ ] 3.1 创建 `ProjectIntro` 组件，使用语义化 HTML 结构承载标题和描述。
- [ ] 3.2 为模块设计渐变背景，选用符合项目调性的现代感颜色。
- [ ] 3.3 确保组件在不同屏幕尺寸下的响应式布局（标题字号缩减、间距调整）。

## 4. 筛选器 (Filter) 增强

- [ ] 4.1 重构 `FilterSection` 组件，计算所有气泡的宽度，限制展示行数为 2 行。
- [ ] 4.2 开发 `FilterOverlay` 组件（使用 `antd` 的 `Modal` 或 `Drawer`）。
- [ ] 4.3 在浮层内实现 A-Z 字母导航条，并关联至下方按字母分组的标签区域（锚点定位）。
- [ ] 4.4 更新 `TagChip` 组件，在标签文本后方添加计数显示，例如 `React (12)`。

## 5. 整合与验证

- [ ] 5.1 运行 `npm run lint` 检查代码规范。
- [ ] 5.2 在深色模式和浅色模式下手动验证页面布局的完整性。
- [ ] 5.3 检查移动端视图下的浮层和 GitHub 导航显示是否正常。
