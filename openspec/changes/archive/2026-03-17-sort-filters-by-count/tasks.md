## 1. 数据层修改

- [ ] 1.1 修改 `src/lib/data.js` 中的 `getTagStats` 函数。
- [ ] 1.2 更新排序逻辑：首先按 `count` 降序排列，由于 `count` 可能相同，次要排序按 `name` 升序（字母顺序）排列。

## 2. 验证与优化

- [ ] 2.1 启动开发服务器并检查首页筛选栏的标签顺序是否已更新。
- [ ] 2.2 确保“更多筛选”浮层中的标签仍然保持字母 A-Z 分组排序，不受此更改影响。
- [ ] 2.3 运行 `npm run lint` 确保代码规范。
