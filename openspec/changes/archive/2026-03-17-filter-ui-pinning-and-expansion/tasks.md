## 1. 组件逻辑修改

- [ ] 1.1 修改 `src/components/FilterSection.js`。
- [ ] 1.2 增加 `displayCount` 限制从 15 增加到 40。
- [ ] 1.3 实现 `pinned` (selected) 与 `unpinned` 的数据分离与合并显示逻辑。

## 2. 验证与优化

- [ ] 2.1 验证选中标签后，标签是否移动到首部。
- [ ] 2.2 验证默认显示的行数是否增加。
- [ ] 2.3 运行 `npm run lint`。
