## 1. UI 修正

- [ ] 1.1 修改 `src/components/GithubNav.js` 中的 Star 数量为 "1"。
- [ ] 1.2 修改 `src/components/RepoCard.module.css`，在 `.footer` 中添加 `align-items: center;`。

## 2. 功能优化

- [ ] 2.1 确认 `src/components/Showcase.js` 中的 `handleExport` 逻辑。
- [ ] 2.2 添加空结果导出的保护逻辑（可选）。

## 3. 验收

- [ ] 3.1 验证本地运行效果，确认 Star 数、对齐情况和导出数据的准确性。
- [ ] 3.2 运行 `npm run lint`。
