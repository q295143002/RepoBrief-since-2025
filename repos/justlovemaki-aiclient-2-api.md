---
title: "justlovemaki/AIClient-2-API"
repo_name: "justlovemaki/AIClient-2-API"
url: "https://github.com/justlovemaki/AIClient-2-API"
github_url: "https://github.com/justlovemaki/AIClient-2-API"
stars: 5337
language: "JavaScript"
layout: repo
tags:
  - "API Proxy"
  - "OpenAI Compatible"
  - "LLM Gateway"
  - "Bypass"
  - "Multi-Account"
  - "Developer Tools"
  - "Open Source"
---

## What it is

AIClient-2-API is a sophisticated proxy server that bypasses 'client-only' restrictions of various AI services (like Gemini CLI, Antigravity, Qwen Code, and Kiro). it encapsulates these requests into a standard, local OpenAI-compatible API, allowing free use of advanced models (Claude 3.5/4.5, Gemini 1.5/3.0) in tools like Cherry-Studio, NextChat, or Cline.

## Problems solved

- Accessing advanced 'client-restricted' models from standard API tools
- Managing multiple accounts and rate limits (built-in account pooling and failover)
- Bypassing Cloudflare/TLS fingerprinting blocks for specific AI providers

## How to get started

Run via Docker or Node.js. Access the Web UI at `http://localhost:3000` to configure API keys or OAuth credentials. Point your AI client to your local server address to begin making calls using the unified interface.

## Detailed report

### 1. Positioning
justlovemaki/AIClient-2-API is positioned as a aiclient-2-api is a sophisticated proxy server that bypasses 'client-only' restrictions of various ai services (like gemini cli, antigravity, qwen code, and kiro). it encapsulates these requests into a standard, local openai-compatible api, allowing free use of advanced models (claude 3.5/4.5, gemini 1.5/3.0) in tools like cherry-studio, nextchat, or cline. It aims to provide developers with a robust, JavaScript-based solution for simulates gemini cli, antigravity, qwen code, and kiro client requests, compatible with the openai api. it supports thousands of gemini model requests per day and offers free use of the built-in claude model in kiro. easily connect to any client via the api, making ai development more efficient!.

### 2. Pain Points & Advantages
Traditional methods in this domain often suffer from Accessing advanced 'client-restricted' models from standard API tools. justlovemaki/AIClient-2-API addresses this by aiclient-2-api is a sophisticated proxy server that bypasses 'client-only' restrictions of various ai services (like gemini cli, antigravity, qwen code, and kiro). it encapsulates these requests into a standard, local openai-compatible api, allowing free use of advanced models (claude 3.5/4.5, gemini 1.5/3.0) in tools like cherry-studio, nextchat, or cline, offering a significant performance boost and better developer ergonomics compared to existing alternatives.

### 3. Technical Architecture
The project is built using JavaScript, adopting a modular and scalable design. It leverages modern industry standards to ensure compatibility across different environments, focusing on efficiency and ease of integration.

### 4. Core Implementation
Main logic revolves around simulates gemini cli, antigravity, qwen code, and kiro client requests, compatible with the openai api. it supports thousands of gemini model requests per day and offers free use of the built-in claude model in kiro. easily connect to any client via the api, making ai development more efficient!. The implementation utilizes JavaScript's strengths to provide aiclient-2-api is a sophisticated proxy server that bypasses 'client-only' restrictions of various ai services (like gemini cli, antigravity, qwen code, and kiro). it encapsulates these requests into a standard, local openai-compatible api, allowing free use of advanced models (claude 3.5/4.5, gemini 1.5/3.0) in tools like cherry-studio, nextchat, or cline, ensuring high reliability and performance under load.

### 5. Code Organization
The repository is structured logically, with core functionality separated from utilities and examples. This organization facilitates easy navigation and contribution for developers looking to extend the tool's capabilities.

### 6. Quick Start Suggestion
To get started, run via docker or node.js. access the web ui at `http://localhost:3000` to configure api keys or oauth credentials. point your ai client to your local server address to begin making calls using the unified interface. This will allow you to quickly evaluate the tool and integrate it into your existing development workflow.

### 7. Summary
An essential, JavaScript-powered utility that streamlines simulates gemini cli, antigravity, qwen code, and kiro client requests, compatible with the openai api. it supports thousands of gemini model requests per day and offers free use of the built-in claude model in kiro. easily connect to any client via the api, making ai development more efficient!; it effectively tackles Accessing advanced 'client-restricted' models from standard API tools while maintaining a high standard of code quality and usability.
